#pragma once

#ifndef RESULT_VARIABLES_H
#define RESULT_VARIABLES_H

#include <vector>

// Declare the global variables here, so we can initialize them in form and use them wherever.
extern int classCount;                      // Integer variable
extern std::vector<char*> classNames;       // Vector of char*'s to store the target class names
extern int dimension;
#endif
